@model InnDocs.iHealth.Web.UI.Models.SingleRegisterModel
@{
    ViewBag.Title = "SubscribeNow";
}

<script type="text/javascript">
    $(function () {
        $("#casesSubscribe").click(function () {
            var uId = $("#uid").val();
            // alert("btnPay clicked");
            // alert(uId);

            $.ajax({
                url: '/iHealthUser/OnlinePaymentUpdate/UpdatePayment', //Update Payment Related
                type: 'GET',
                // data: { UID: uId },
                success: function (result) {
                    //$.modal.close();
                   // alert(result);
                    $('#ajaxtab').html(result).modal({
                        escClose: true,
                        closeHTML: "",
                        onShow: function (e) {
                            e.container.css({ 'height': 'auto', 'width': 'auto' });
                        }
                    });
                }
            });

            $('#btnCancel').click(function () {
                $.modal.close();
                return false;
            });
        });
    });

//    //added by kumar for progress bar
//    $(window).load(function () {
//        var name = document.getElementById('imgname').value;
//        // alert("hi " + name);
//        var day = document.getElementById('currentday').value;
//        //alert("Current day " + name2);
//        var day1 = (day / 280) * 100;
//        // alert(parseInt(day));
//        var edd = document.getElementById('EDDdate').value;

//        // alert("Edd date " + edd);
//        $("#progressbar").progressbar({
//            value: parseInt(day1)
//        });
//    });
//    //end here
    
</script>
@*@section RightPanel{
    @{string code = ViewBag.EDDdate.ToString();
      string[] tmpe;
      string duedate;

      tmpe = code.Split(' ');

      duedate = tmpe[0].ToString();

    }
    @MvcHelper.Rightpanel(ViewBag.ImageName, duedate)
}*@

<input type="button" id="casesSubscribe" value="Subscribe Now" class="greengrad" style="float:left; width:110px;"  />
 <input type="hidden" id="imgname" value="@ViewBag.ImageName" />
    <input type="hidden" id="currentday" value="@ViewBag.currentday" />
    <input type="hidden" id="EDDdate" value="@ViewBag.EDDdate" />

@*Free Trial (@Html.DisplayTextFor(model => model.Result) days left) <a style="color:Red" href="@Url.Action("PaymentManagement", "AccountSettings")" >
    Subscribe Now</a> *@
    <script>
        (function (i, s, o, g, r, a, m) {
            i['GoogleAnalyticsObject'] = r; i[r] = i[r] || function () {
                (i[r].q = i[r].q || []).push(arguments)
            }, i[r].l = 1 * new Date(); a = s.createElement(o),
  m = s.getElementsByTagName(o)[0]; a.async = 1; a.src = g; m.parentNode.insertBefore(a, m)
        })(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');

        ga('create', 'UA-47299480-1', 'bumpdocs.com');
        ga('send', 'pageview');

</script>
