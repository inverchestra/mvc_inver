@model PagedList.IPagedList<InnDocs.iHealth.Web.UI.Areas.iHealthUser.Models.UserRepository.Cases>
@{
    ViewBag.Title = "Cases";
}
<h2>
    Cases</h2>
<script type="text/javascript">
    history.go(1);
//    //added by kumar for progress bar
//    $(window).load(function () {
//        var name = document.getElementById('imgname').value;
//        // alert("hi " + name);
//        var day = document.getElementById('currentday').value;
//        //alert("Current day " + name2);
//        var day1 = (day / 280) * 100;
//        // alert(parseInt(day));
//        var edd = document.getElementById('EDDdate').value;

//        // alert("Edd date " + edd);
//        $("#progressbar").progressbar({
//            value: parseInt(day1)
//        });
//    });
//    //end here
</script>
@section Search{
    <div id="bottom-head">
        <div id="search">
            <form id="searchform" method="post" action="@Url.Action("GetSearchedCases", "Cases")">
            <div class="search_pnl">
                <input type="text" name="searchtext1" />
                <input type="submit" value="" title="Search" />
            </div>
            </form>
        </div>
    </div>
}
@*@section RightPanel{
    @{string code = ViewBag.EDDdate.ToString();
      string[] tmpe;
      string duedate;

      tmpe = code.Split(' ');

      duedate = tmpe[0].ToString();

    }
    @MvcHelper.Rightpanel(ViewBag.ImageName, duedate)
}*@
<script type="text/javascript" src="~/CustomScripts/table-paging.js"></script>
<script type="text/javascript" src="~/CustomScripts/table-toggle.js"></script>
<div id="table-wrapper">
    <div id="div2">
        @RenderPage("~/Areas/iHealthUser/Views/Shared/_TableCase.cshtml", new { Values = Model })
    </div>
    <table>
        <tfoot>
            <tr>
                <td>
                    <a href="@Url.Action("CreateCase", "Cases")" id="btnCreate" class="createcase" >CREATE
                        CASE</a>
                </td>
            </tr>
        </tfoot>
    </table>
    <input type="hidden" id="imgname" value="@ViewBag.ImageName" />
    <input type="hidden" id="currentday" value="@ViewBag.currentday" />
    <input type="hidden" id="EDDdate" value="@ViewBag.EDDdate" />
</div>
<script>
    (function (i, s, o, g, r, a, m) {
        i['GoogleAnalyticsObject'] = r; i[r] = i[r] || function () {
            (i[r].q = i[r].q || []).push(arguments)
        }, i[r].l = 1 * new Date(); a = s.createElement(o),
  m = s.getElementsByTagName(o)[0]; a.async = 1; a.src = g; m.parentNode.insertBefore(a, m)
    })(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');

    ga('create', 'UA-47299480-1', 'bumpdocs.com');
    ga('send', 'pageview');

</script>
