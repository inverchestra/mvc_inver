@model IEnumerable<InnDocs.iHealth.Web.UI.Areas.iHealthUser.Models.UserRepository.GrowthScan>
<script type="text/javascript">
    $(function () {
        $('input[name="growthscan"]').click(function () {
            var growthid = $(this).siblings(':hidden').val();

            $.ajax({
                url: '/DoulaBumpUser/DoulaMedwall/GrowthScanview',
                type: 'GET',
                data: { growthid: growthid },
                success: function (result) {
                    $('#ajaxtab').html(result).modal({
                        escClose: true,
                        position: ["2%", "30%"],
                        closeHTML: "",
                        onShow: function (e) {
                            e.container.css({ 'height': 'auto', 'width': 'auto' });
                        }

                    });
                }
            });
        });


    });
</script>
<div id="proctable">
    <div>
       
    </div>
    @if (Model.Count() > 0)
    {     
        <ul class="li_color" style="margin:0px">
                    @foreach (var item in Model)
                    {
                        <li>
                        @item.CreatedOn
                        <div style="float:right; display: inline-block">
                            <input type="button" id="growthscan" name="growthscan" class="viewbutton" value="View" />
                            <input type="hidden" id="growthid" value="@item.GrowthScanId" />
                        </div>
                            </li>
                    }
            </ul>
    }
    else
    {
        <text>No scans</text>
    }
</div>


<div id="tab" style="display: none">
</div>
<script>
    (function (i, s, o, g, r, a, m) {
        i['GoogleAnalyticsObject'] = r; i[r] = i[r] || function () {
            (i[r].q = i[r].q || []).push(arguments)
        }, i[r].l = 1 * new Date(); a = s.createElement(o),
  m = s.getElementsByTagName(o)[0]; a.async = 1; a.src = g; m.parentNode.insertBefore(a, m)
    })(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');

    ga('create', 'UA-47299480-1', 'bumpdocs.com');
    ga('send', 'pageview');

</script>
